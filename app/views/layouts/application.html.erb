<!DOCTYPE html>
<html>
  <head>
    <title>Tacostagram Final</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <link rel="stylesheet" href="/stylesheets/application.css">
    <!-- Bootstrap (Ensure it's included for styling) -->
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css">
  </head>

  <body>
    <div class="container mt-3">
      <%= flash["notice"] %>
    </div>

    <!-- Bootstrap Navbar -->
    <nav class="navbar navbar-expand-lg navbar-light bg-light">
      <div class="container">
        <a class="navbar-brand" href="/">Home</a>

        <div class="collapse navbar-collapse">
          <ul class="navbar-nav ms-auto">
            <% if session[:user_id] %>
              <% user = User.find_by(id: session[:user_id]) %>
              <% if user %>
                <li class="nav-item">
                  <span class="navbar-text">Welcome, <%= user.email %></span>
                </li>
                <li class="nav-item">
                  <a class="btn btn-danger ms-2" href="/logout">Logout</a>
                </li>
              <% end %>
            <% else %>
              <li class="nav-item">
                <a class="btn btn-primary me-2" href="/login">Login</a>
              </li>
              <li class="nav-item">
                <a class="btn btn-success" href="/users/new">Signup</a>
              </li>
            <% end %>
          </ul>
        </div>
      </div>
    </nav>

    <div class="container mt-4">
      <%= yield %>
    </div>
  </body>
</html>
